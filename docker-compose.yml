version: '3.8'

services:
  adrian-bot:
    build: .
    container_name: adrian-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
